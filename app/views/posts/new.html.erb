 <h1>Write New Post</h1>

<%= form_for @post do |t| %>
	<%= t.label :title %>
	<%= t.text_field :title %>
	<%= t.label :body %>
	<%= t.text_field :body %>
	<%= t.submit :create %>
<% end %>

<p><%= link_to "Home", posts_path %></p>
